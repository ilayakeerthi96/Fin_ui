export const environment = {
  production: true,

  // The API gateway, same as in environment.ts — only the host differs per environment.
  //API_URL: 'http://10.0.1.26:8089/',
  API_URL: 'http://117.239.138.177:8080/',
};
